# API Configuration
VITE_API_BASE=http://3.36.49.60:8080

# For production deployment (optional)
# VITE_API_BASE=https://your-production-api.com

# If API Gateway double-decodes path params, enable this at build time
# to double-encode client path segments (mitigates 400s for spaces/specials).
# VITE_DOUBLE_ENCODE_PATHS=1

# Enable client-side API logging for troubleshooting (URL, method, status, ms)
# VITE_API_DEBUG=1
